# Render 部署配置
# 文档: https://render.com/docs/blueprint-spec

services:
  - type: web
    name: ai-api-proxy
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 10000
